## Flask + Vue minimal environment

### Flask

```
cd server
pipenv install
pipenv shell

export FLASK_APP=ner_project
export FLASK_ENV=development
flask run
```

### Vue

```
cd client
yarn # or npm install
yarn serve
```
